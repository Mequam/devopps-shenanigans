/*
 * this class is used when retriving all quizis from the database as a way
 * to indicate that this is a quiz, and that further information can be queried
 * */
export class QuizFingerprint {
  public readonly id : number = 0;
  public readonly name : string = "";
}
